# dependencies
node_modules/
.pnp.*

# build / cdk
dist/
cdk.out/
web/dist/
*.tsbuildinfo

# env & secrets — never commit
.env
.env.*
!.env.local.example
*.pem
*.key

# agent scratch space (AGENTS.md) — notes, captured logs, intermediate JSON
.agents/

# logs / os
*.log
.DS_Store

# ide
.idea/
.vscode/

# agent scratch space (notes, probe scripts, captured logs — never a deliverable)
.agents/
